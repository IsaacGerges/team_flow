import 'package:get_it/get_it.dart';

final sl = GetIt.instance; // sl = Service Locator

Future<void> init() async {
  // هنا هنسجل كل الـ Features بتاعتنا لما نبنيها
  // Features - Auth

  // Core

  // External (Firebase instances)
}
